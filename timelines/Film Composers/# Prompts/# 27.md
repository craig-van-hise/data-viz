
### 🤖 AGENT TASK: Patch David Arnold & Populate Audio Links

**Context for Agent:** We encountered the "blank array / Cannot read properties of undefined (reading 'year')" error for David Arnold. This is the same TMDB filtering/identification quirk we solved for Michel Legrand. We need to fetch his correct filmography using our broadened job filters and then fetch his audio links.

**Your Task:**

**1. Update & Run the Patch Script**
Open the existing `scripts/patch_tmdb_filmographies.py` file.
* Change the target list to strictly process him: `composers = ["David Arnold"]`.
* **Important:** Because "David Arnold" is a common name, the default `search/person` API call might grab the wrong person. Write a quick temporary python snippet to hit the TMDB API using our `.env` token to find the exact Person ID for the David Arnold who composed *Independence Day* and *Casino Royale*. 
* Add his ID to the `TMDB_ID_OVERRIDES` dictionary inside the patch script.
* Run `scripts/patch_tmdb_filmographies.py` and verify `data/filmographies/David_Arnold.json` is now populated with his actual films, not a blank array.

**2. Scrape and Verify YouTube Links**
Now that `David_Arnold.json` is populated with TMDB data, it currently has no YouTube links.
* Run our unified `scripts/scrape_and_verify_links.py`.
* Because of the "Robust Threshold" logic we implemented earlier, the script will automatically detect that David Arnold's file has 0 valid links and will automatically process his entire filmography. You do not need to modify the scraper script.

**3. Final Verification**
Verify that `David_Arnold.json` contains valid YouTube URLs and `null` values where appropriate. Report back to the user when the fix is complete.

