
# Agent Task: Dynamic Track Margin for Waveform Timelines

**Context:** We recently updated `composer_profile.html` to group timeline markers into vertical stacks (columns) that grow upwards and downwards from the center, creating a waveform aesthetic. However, for highly prolific years (e.g., 17 films), the upward-growing dots collide with and breach the horizontal `.year-axis` above it. We need to dynamically push the `.timeline-track` down based on the maximum stack height so the tallest peak always rests safely below the axis.

**File to Modify:** `composer_profile.html`

**Instructions:**

1. **Locate the `drawMarkers` function.** 2. Find the point in the code immediately *after* the `columns` array has been fully populated, but *before* the `columns.forEach` loop that renders the dots to the DOM.
3. **Insert the following dynamic clearance calculation:**

```javascript
            // 1.5 Calculate dynamic top margin to prevent axis collision
            if (columns.length > 0) {
                const maxStack = Math.max(...columns.map(col => col.length));
                const spacing = 16; // Vertical pixels between dots
                
                // Calculate how far up the tallest peak will reach
                const maxUpwardReach = (maxStack / 2) * spacing;
                
                // Base margin is 20px. Add a 15px visual buffer to the required clearance.
                const safeMargin = Math.max(20, maxUpwardReach + 15);
                
                // Apply the calculated margin to the track to push the entire waveform down
                track.style.marginTop = `${safeMargin}px`;
            }

            // 2. Distribute each column vertically from the center
            columns.forEach(col => {
                // ... (Existing column rendering logic remains identical here) ...
```

**Technical Requirements for the Agent:**
* **Placement:** This math must execute exactly between step 1 (grouping columns) and step 2 (distributing them). 
* **Variable Consistency:** Ensure the `spacing` variable in the clearance calculation matches the `spacing` variable inside the `columns.forEach` rendering loop (currently `16`).
* **CSS Impact:** This modifies `track.style.marginTop` inline. The `.timeline-track` in the CSS has a default `margin-top: 20px`, which this script will now smoothly override for denser timelines like Max Steiner's, while leaving less prolific composers unaffected.