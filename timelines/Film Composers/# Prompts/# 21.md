

### Agent Task Prompt

**Context:**
Our `scripts/audit_youtube_links.py` script is throwing a "Requested format is not available" error when attempting to extract metadata while authenticated. We need to force `yt-dlp` to only scrape the top-level webpage metadata and ignore the video streams entirely to prevent this crash.

Please update `scripts/audit_youtube_links.py` with the following change:

#### Task 1: Force yt-dlp to Skip Media Processing
Locate the line inside the main loop where the metadata is extracted:
```python
info = ydl.extract_info(youtube_link, download=False)
```

Update this line to include `process=False`. This forces `yt-dlp` to strictly extract the webpage dictionary (which contains the title and description) without attempting to resolve the playable media streams:
```python
info = ydl.extract_info(youtube_link, download=False, process=False)
```

