

### Agent Task Prompt

**Context:**
Our `scripts/audit_youtube_links.py` script is still hitting aggressive HTTP 429 Rate Limits from YouTube despite our throttling efforts. We are going to bypass this by authenticating the `yt-dlp` session using the user's local Google Chrome cookies.

Please update `scripts/audit_youtube_links.py` with the following targeted changes:

#### Task 1: Inject Browser Cookies into yt-dlp
Locate the `ydl_opts` dictionary in the script (the configuration object passed into `yt_dlp.YoutubeDL()`). 
Add the following key-value pair to the dictionary:
```python
'cookiesfrombrowser': ('chrome',),
```
*Note: This tells yt-dlp to automatically locate the local Chrome cookie database, decrypt it, and use it for the session.*

#### Task 2: Adjust Throttling for Authenticated State
Because the session is now authenticated, we no longer need extreme cooldowns, but we should still be polite.
1. Remove the 60-second "coffee break" cooldown logic entirely.
2. Change the randomized sleep delay between standard requests to a much shorter, reasonable interval:
```python
time.sleep(random.uniform(1.5, 3.0))
```

#### Task 3: Add a Keychain Warning Log
Just before the script initializes `yt_dlp.YoutubeDL(ydl_opts)`, add a prominent `print()` statement to the console warning the user about the macOS security prompt:
```python
print("\n[!] Attempting to authenticate using Google Chrome cookies.")
print("[!] If you are on a Mac, you may see a system prompt asking for your keychain password.")
print("[!] Please enter your password and click 'Allow' or 'Always Allow' to proceed.\n")
```

Please execute these modifications to `scripts/audit_youtube_links.py`.