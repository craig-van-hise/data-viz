You are an expert Python developer working on my "Film Composers Timeline" project. 

**The Problem:**
Our current JSON files in `data/filmographies/` contain corrupted and bloated data because they were generated using unreliable web scraping methods. We are abandoning the scraping approach for filmographies and moving to the TMDb API to get pristine, structured data.

**The Task:**
I need you to write, install dependencies for, and execute a new Python script located at `scripts/fetch_tmdb_filmographies.py`. This script will use the TMDb API to rebuild the filmography JSON files for every composer in our list, overwriting the old, bad data.

**Requirements & Logic:**
1. **Dependencies:** Ensure `requests` and `python-dotenv` are installed in the environment.
2. **Authentication:** Load the `TMDB_API_READ_ACCESS_TOKEN` from the `.env` file in the project root. Use it as a Bearer token in the headers: `{"Authorization": f"Bearer {token}", "accept": "application/json"}`.
3. **Data Source:** Read the list of composer names from `data/Film Composers list.json`.
4. **Execution Loop:** For each composer in the list:
    * **Step A (Find ID):** Make a GET request to `https://api.themoviedb.org/3/search/person?query={url_encoded_name}&include_adult=false`. Extract the `id` of the first result. (If no results, log a warning and skip).
    * **Step B (Get Credits):** Make a GET request to `https://api.themoviedb.org/3/person/{id}?append_to_response=movie_credits&language=en-US`.
    * **Step C (Filter):** Extract the `movie_credits.crew` array. Filter this array strictly to keep ONLY objects where `department == "Sound"` AND `job == "Original Music Composer"`. This prevents bad data collisions.
    * **Step D (Format):** Map the filtered data into a clean dictionary format containing:
        - `title`: The film's title.
        - `year`: The year parsed from `release_date` (If `release_date` is empty or missing, assign `9999`).
        - `id`: The movie's TMDb ID.
        - `poster`: If `poster_path` exists, prepend it with `https://image.tmdb.org/t/p/w200`. Otherwise, set to `null`.
    * **Step E (Clean & Sort):** Deduplicate the list based on the movie `id` (to avoid duplicate entries if a composer had two roles). Sort the list chronologically by `year`.
    * **Step F (Save):** Save the clean list as a JSON array to `data/filmographies/{Composer_Name_With_Underscores}.json` (e.g., replace spaces in the name with underscores to match the existing file structure like `data/filmographies/Hans_Zimmer.json`). Overwrite the existing file.
    * **Step G (Rate Limiting):** Add a `time.sleep(0.3)` at the end of the loop to respect TMDb's rate limits (max 40 requests per 10 seconds).

Please write the script, output the code for my review, and then execute it to rebuild the `data/filmographies/` directory.