
You are an expert developer working on my "Film Composers Timeline" project. We have three tasks to execute immediately to fix issues caused by our recent data update.

**Task 1: Fix the HTML Image Fallback**
Open `composer_profile.html`. Inside the `drawMarkers` function, locate the `films.forEach(film => {` loop. 
Update the image logic so that it tries to load the local thumbnail first. If that fails (onerror), it must fall back to the `film.poster` URL from the JSON, and finally to a generic placeholder.

Replace the current `let html = ` block with this exact code:

```javascript
                // Image Fallback Logic
                const localThumb = `${thumbBase}${safeTitle}.jpg`;
                const tmdbThumb = film.poster ? film.poster : '[https://placehold.co/200x300?text=No+Image](https://placehold.co/200x300?text=No+Image)';

                let html = `
                    <div class="film-tooltip">
                        <img src="${localThumb}" class="tooltip-img" onerror="this.onerror=null; this.src='${tmdbThumb}';">
                        <div class="tooltip-title">${film.title}</div>
                        <div class="tooltip-year">${dateVal}</div>
                `;
                if (film.youtube_link) {
                    html += `<a href="${film.youtube_link}" target="_blank" class="tooltip-meta">▶ Play Theme</a>`;
                    marker.onclick = () => window.open(film.youtube_link, '_blank');
                }
                html += `</div>`;
                marker.innerHTML = html;
```

**Task 2: Fix the TMDb Script (Stop Overwriting Data)**
Open the Python script we use to fetch TMDb data (`scripts/fetch_tmdb_filmographies.py`). Modify the file-saving logic so it no longer blindly overwrites the existing JSON. 
* It must load the existing JSON file (if it exists).
* It must compare the new TMDb films against the existing films.
* If an existing film has a `youtube_link`, it MUST preserve that key and merge it into the new TMDb film object before saving. 

**Task 3: Repopulate YouTube Links**
Because our previous run wiped out the `youtube_link` data in `data/filmographies/`, we need to repopulate it.
Run `python scripts/enrich_youtube_links.py` (or the equivalent script that loops through the filmographies) to dynamically fetch and inject the YouTube links back into the JSON files.

Please execute these three tasks now and confirm when complete.
