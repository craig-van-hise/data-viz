
# Agent Task: Invert Highlight Labels & Implement Edge Bounding

**Context:** We need to re-engineer the "Most Important Films" labels. They currently grow upward and collide with the timeline axis. We are moving them below the waveform, hanging downwards. Additionally, labels near the edges of the timeline (like Alex North's 1951 entry) are clipping off the screen. We need to update the HTML structure, reverse the CSS/JS collision logic to build downward, and add dynamic screen-edge bounding.

**File to Modify:** `composer_profile.html`

**Instructions:**

### 1. Update the HTML Structure inside `drawMarkers`
Locate the block in `drawMarkers` where the highlight container is created (around line 348). We need to swap the order of the pole and label so the pole originates at the dot.
**Change this:**
```javascript
                    highlight.innerHTML = `
                        <div class="marker-label">${film.title}</div>
                        <div class="marker-pole"></div>
                    `;
```
**To this:**
```javascript
                    highlight.innerHTML = `
                        <div class="marker-pole"></div>
                        <div class="marker-label">${film.title}</div>
                    `;
```

### 2. Update the CSS for Downward Projection
Locate `.marker-imp-container` in the `<style>` block.
**Change `bottom: 40px;` to `top: 15px;`** so it hangs off the bottom of the dot.
*(Leave the rest of the properties in that CSS class intact).*

### 3. Rewrite the Collision Resolution Engine
Locate the `resolveSmartCollisions` function. Replace the lower half of the function (everything inside the `containers.forEach(box => { ... })` loop *after* the lane occupancy registration) with this updated logic that builds downwards and prevents screen edge clipping:

```javascript
                // Register occupancy
                lanes[laneIndex].push({ left, right });

                // 1. Vertical Stacking (Building Downwards)
                const baseTop = 15; // Starting distance below the dot
                const step = 38; // Height of each lane
                
                // Adjust container top
                box.style.top = `${baseTop}px`;
                box.style.bottom = 'auto'; // Clear old inline styles if any

                // Calculate required pole height to push label down into its lane
                const basePole = 25;
                const newPoleHeight = basePole + (laneIndex * step);
                pole.style.height = `${newPoleHeight}px`;

                // Boost z-index to keep lower hanging labels on top of overlapping poles
                box.style.zIndex = 100 + laneIndex;

                // 2. Horizontal Edge Bounding (Prevent screen clipping)
                const viewportWidth = window.innerWidth;
                const safetyPadding = 15; // Pixels from the edge of the screen
                let currentTransform = -50; // Default translateX percentage
                let pixelOffset = 0; // Adjustment needed

                // Check Left Edge
                if (left < safetyPadding) {
                    pixelOffset = safetyPadding - left;
                } 
                // Check Right Edge
                else if (right > (viewportWidth - safetyPadding)) {
                    pixelOffset = (viewportWidth - safetyPadding) - right;
                }

                // Apply bounding adjustment if necessary while maintaining center point
                if (pixelOffset !== 0) {
                    box.style.transform = `translateX(calc(${currentTransform}% + ${pixelOffset}px))`;
                } else {
                    box.style.transform = `translateX(${currentTransform}%)`;
                }
```

### 4. Ensure Bottom Container Padding
In the `<style>` block, check the `#timeline-container`. Ensure `padding-bottom` is at least `250px` to give these downward-hanging labels plenty of room to stack without causing scrollbar jitter.

