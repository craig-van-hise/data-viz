

### 🤖 AGENT TASK: Create & Run Targeted TMDB Patch Script

**Context for Agent:** We have three composers ("Michel Legrand", "Tangerine Dream", "Ryuichi Sakamoto") whose filmography JSON files are generating as empty arrays `[]` due to overly strict TMDB job filters and bad text-search ID matching. We need to fix this without touching the main production script (`scripts/fetch_tmdb_filmographies.py`).

**Your Task:**
Create a new file called `scripts/patch_tmdb_filmographies.py`. This script should be a modified clone of `fetch_tmdb_filmographies.py`, strictly tailored to patch these three artists.

**1. Create the Patch Script (`scripts/patch_tmdb_filmographies.py`)**
Copy the base logic from `fetch_tmdb_filmographies.py`, but implement the following changes:

* **Hardcode the Target List:** Remove the code that loads `Film Composers list.json`. Instead, explicitly define the list: 
  `composers = ["Michel Legrand", "Tangerine Dream", "Ryuichi Sakamoto"]`
* **Implement the ID Overrides:** Near the top of the script, create this dictionary:
  `TMDB_ID_OVERRIDES = {"Michel Legrand": 2492, "Tangerine Dream": 59302, "Ryuichi Sakamoto": 4030}`
* **Apply the Overrides (Step A):** Before doing the `requests.get` to search for the person, check if the composer is in `TMDB_ID_OVERRIDES`. If they are, use that ID directly and skip the search API call.
* **Broaden the Job Filter (Step C):** Update the list comprehension. Instead of strictly requiring `"Original Music Composer"`, accept multiple standard TMDB variations:
  `item.get("job") in ["Original Music Composer", "Music", "Composer"]`

**2. Run the Patch Script**
Execute `python scripts/patch_tmdb_filmographies.py`. 

**3. Verify**
Check `data/filmographies/Michel_Legrand.json`, `Tangerine_Dream.json`, and `Ryuichi_Sakamoto.json`. Confirm they are now populated with actual film data instead of empty arrays. Report back to the user when complete.

