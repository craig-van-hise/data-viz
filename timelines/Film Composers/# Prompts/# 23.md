

### Agent Task Prompt

**Context:**
The script is failing to dynamically update the `.audit_checkpoint` file while running. It is buffering the writes in memory, meaning if the script stops unexpectedly, hours of checkpoint progress are lost. We need to force the OS to write to the disk immediately after every single file.

Please update `scripts/audit_youtube_links.py` with the following changes:

#### Task 1: Force Instant Checkpoint Writes
1. Ensure `import os` is at the top of the file.
2. Locate the block at the bottom of the `for filename in files:` loop where the script updates the checkpoint file. 
3. **Replace** that block with this exact code:

```python
            # Mark file as processed and FORCE flush to hard drive immediately
            with open(checkpoint_file, 'a') as f:
                f.write(f"{filename}\n")
                f.flush()
                os.fsync(f.fileno())
            print(f"  [Checkpoint Locked: {filename}]")
```

Please execute this change immediately. Every single time a composer file finishes, the script must physically write it to the checkpoint file before moving to the next one.