Instructions: open `composer_profile.html` and replace the `initTimeline`, `getPercent`, and `drawMarkers` functions with the following updated code:**

```javascript
        function initTimeline(data, highlightList = []) {
            console.log("Timeline init v1.65. Highlights:", highlightList);
            
            // FIX 1: Support the new pure array structure from TMDb
            const films = Array.isArray(data) ? data : data.filmography;
            
            const concertKeywords = ["Op.", "Concerto", "Sonata", "Symphony", "Quartet", "Trio", "Quintet", "Variations", "Motet", "Cantata", "Mass", "Psalm", "Overture"];
            const junkKeywords = ["Articles lacking sources", "Category:", "Template:"];

            const meta = COMPOSER_META[composerName] || { death: 9999 };

            const validFilms = films.filter(f => {
                // FIX 2: Check for the new 'year' integer or fall back to old 'release_date'
                const releaseYear = f.year || (f.release_date ? parseInt(f.release_date.substring(0, 4)) : null);
                
                if (!releaseYear || releaseYear === 9999) return false;

                // Exclude concert works
                if (concertKeywords.some(kw => f.title.includes(kw))) return false;

                // Exclude junk
                if (junkKeywords.some(kw => f.title.includes(kw))) return false;

                // Exclude post-death works
                if (releaseYear > meta.death) return false;

                return true;
            });

            // FIX 3: Sort using the integer year
            validFilms.sort((a, b) => {
                const yearA = a.year || parseInt(a.release_date.substring(0, 4));
                const yearB = b.year || parseInt(b.release_date.substring(0, 4));
                return yearA - yearB;
            });

            // Stats (FIX 4: use integer year)
            const startYear = validFilms[0].year || parseInt(validFilms[0].release_date.substring(0, 4));
            const endYear = (validFilms[validFilms.length - 1].year || parseInt(validFilms[validFilms.length - 1].release_date.substring(0, 4))) + 1;
            
            document.getElementById('composer-stats').textContent = `${validFilms.length} Films | Active ${startYear} - ${endYear - 1}`;

            drawAxis(startYear, endYear);
            drawMarkers(validFilms, startYear, endYear, highlightList);
        }

        // FIX 5: Update getPercent to handle simple integers
        function getPercent(dateVal, start, end) {
            let decimalYear;
            if (typeof dateVal === 'number') {
                decimalYear = dateVal; // new TMDb format
            } else {
                // old string format
                const y = parseInt(dateVal.substring(0, 4));
                let m = 0;
                let d = 0;

                if (dateVal.length >= 7) m = parseInt(dateVal.substring(5, 7)) - 1;
                if (dateVal.length >= 10) d = parseInt(dateVal.substring(8, 10)) - 1;

                const dayOfYear = (m * 30) + d;
                decimalYear = y + (dayOfYear / 365);
            }

            return ((decimalYear - start) / (end - start)) * 100;
        }

        function cleanTitle(t) {
            return t.replace(/['":\.\?!]/g, "").trim();
        }

        function drawMarkers(films, start, end, highlightList = []) {
            const track = document.getElementById('track');
            const markers = []; 

            films.forEach(film => {
                // Use new year or old release_date
                const dateVal = film.year || film.release_date; 
                const pct = getPercent(dateVal, start, end);
                const safeTitle = cleanTitle(film.title);
                const thumbUrl = `${thumbBase}${safeTitle}.jpg`;

                const marker = document.createElement('div');
                marker.className = 'film-marker';
                marker.style.left = `${pct}%`;

                // Content
                let html = `
                    <div class="film-tooltip">
                        <img src="${thumbUrl}" class="tooltip-img" onerror="this.src='https://placehold.co/200x300?text=No+Image'">
                        <div class="tooltip-title">${film.title}</div>
                        <div class="tooltip-year">${dateVal}</div>
                `;
                if (film.youtube_link) {
                    html += `<a href="${film.youtube_link}" target="_blank" class="tooltip-meta">▶ Play Theme</a>`;
                    marker.onclick = () => window.open(film.youtube_link, '_blank');
                }
                html += `</div>`;
                marker.innerHTML = html;

                // (The rest of your existing drawMarkers logic from marker.addEventListener down remains exactly the same!)
```

