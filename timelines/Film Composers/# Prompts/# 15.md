

# Agent Task: Dynamic Container Spacing & Padding Reduction

**Context:** The timeline layout currently has dead space issues. The top space above the flags is hardcoded and does not adjust dynamically based on the height of the waveform or the number of flag lanes. Additionally, there is too much empty padding below the year axis at the bottom. We need to strip the hardcoded CSS padding and implement a dynamic `marginTop` calculation that perfectly hugs the highest flag.

**File to Modify:** `composer_profile.html`

**Instructions:**

### 1. Strip the Hardcoded CSS Spacing
Locate the `<style>` block and update these two selectors to remove the bloated padding/margins:

**Update `#timeline-container`:**
```css
        #timeline-container {
            width: 90%;
            max-width: 1400px;
            margin-top: 60px;
            position: relative;
            padding-top: 20px; /* Removed the hardcoded 140px */
            padding-bottom: 0px; /* Minimized bottom padding */
        }
```

**Update `.year-axis`:**
```css
        .year-axis {
            position: relative;
            height: 40px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            margin-top: 15px; /* Reduced from 30px */
            margin-bottom: 0px;
        }
```

### 2. Tighten the Dynamic Bottom Margin (JS)
Locate the dynamic bottom margin calculation inside `drawMarkers` (around Step 1.5, before the columns loop).
**Reduce the `safeMargin` calculation to tighten the space between the waveform and the axis:**

```javascript
            // 1.5 Calculate dynamic bottom margin to prevent axis collision
            if (columns.length > 0) {
                const maxStack = Math.max(...columns.map(col => col.length));
                const spacing = 16; 
                const maxDownwardReach = (maxStack / 2) * spacing;
                
                // Reduced the fallback base margin and the padding
                const safeMargin = Math.max(10, maxDownwardReach + 10);
                
                // Push the axis down by adding margin-bottom to the track
                track.style.marginBottom = `${safeMargin}px`;
            }
```

### 3. Implement the Dynamic Top Margin Calculation (JS)
Locate the `resolveSmartCollisions` function. Go to the very bottom of this function, just *after* the `boxesData.forEach` loop finishes.

**Inject this block to dynamically calculate and apply the exact `marginTop` needed to fit the flags:**

```javascript
            // 9. Dynamic Top Margin for the Track
            const numLanes = lanes.length;
            if (numLanes > 0 || minMarkerY < trackRect.top) {
                // Calculate the distance from the top of the track to the highest dot
                const upwardReach = Math.max(0, trackRect.top - minMarkerY);
                
                // Calculate flag height: 25px (baseline gap) + 42px per lane + 34px (label height)
                const flagHeight = numLanes > 0 ? 25 + (numLanes * 42) + 34 : 0;
                
                // Set the exact margin needed plus 20px of breathing room
                const requiredTopMargin = upwardReach + flagHeight + 20; 
                document.getElementById('track').style.marginTop = `${requiredTopMargin}px`;
            }
```

