
### 🤖 AGENT TASK: Fix Header Border Collision and Axis Alignment

**Target File:** `composers.html`

**Objective:** Resolve the "broken line" visual glitch caused by overlapping header borders, and properly anchor the year markers so they sit cleanly above the timeline axis.

**Implementation Steps:**

1. **Separate the Overlapping Borders:**
   * Locate the `.timeline-header` CSS class in the `<style>` block.
   * **Remove** the `margin-top: -1rem;` rule entirely. 
   * *Why:* This allows the container to sit naturally below the main header, ensuring the main header's full-width border and the timeline's indented axis border remain parallel and distinct, completely removing the "broken line" illusion.

2. **Anchor the Year Markers Above the Line:**
   * Locate the `.year-marker` CSS class in the `<style>` block.
   * **Add** the property `bottom: 0;` to the rule set.
   * *Why:* The year markers are absolutely positioned but were missing a vertical anchor. Anchoring them to the bottom of the `.header-track` forces them to sit flush above the `.timeline-header`'s bottom border (the axis line), matching the clean aesthetic of the individual profile pages.

**Validation:** After applying, verify that there are two distinct, unbroken lines at the top of the page (one under the main title, one under the years), and that the years (1920, 1930...) no longer float haphazardly through the axis line.

