
### 🤖 AGENT TASK: Redesign Timeline Header and Axis Aesthetics

**Target File:** `composers.html`

**Objective:** Overhaul the CSS of the sticky header region to eliminate the "double line" visual collision, extend the timeline axis to the edges of the viewport, and vertically center the year markers with symmetrical spacing.

**Implementation Steps:**

1. **Remove the Redundant Top Line:**
   * Locate the `header` CSS rule block.
   * Change `border-bottom: 1px solid rgba(255, 255, 255, 0.1);` to `border-bottom: none;`.

2. **Extend and Pad the Timeline Axis:**
   * Locate the `.timeline-header` CSS rule block.
   * Add negative horizontal margins to pull the background/border to the window edges, and compensate with padding so the internal grid stays perfectly aligned with the rows below.
   * Update the rules to include:
     ```css
     margin-left: -2rem;
     margin-right: -2rem;
     padding-left: 2rem;
     padding-right: 2rem;
     padding-top: 1.5rem; /* Add breathing room above the numbers */
     padding-bottom: 0.5rem;
     ```

3. **Float the Year Numbers:**
   * Locate the `.year-marker` CSS rule block.
   * Ensure there is a rule lifting the text off the physical line so it doesn't look squashed.
   * Update or add the property: `bottom: 8px;`

**Validation:** After these CSS changes, there should only be **one** horizontal line visible at the top of the timeline. It should span 100% of the screen width. The year numbers (1920, 1930) should have comfortable, symmetrical dark space above them and float exactly 8 pixels above the line.

