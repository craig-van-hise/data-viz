

### 🤖 AGENT TASK: Implement Dynamic Viewport Collision Engine for Tooltips

**Target Files:**
1. `composers.html`
2. `composer_profile.html`

**Objective:**
Replace the brittle/hardcoded vertical positioning logic for tooltips with a robust, dynamic algorithm that calculates exact viewport boundaries and element dimensions to intelligently switch between "Up" and "Down" states, preventing top-edge clipping.

**Implementation Steps:**

**Phase 1: Refactor `composers.html` (Local Tooltip Collision)**
1. Locate the `marker.addEventListener('mouseenter', ...)` block inside the `createRows()` function.
2. Remove the hardcoded `if (rect.top < 200)` condition.
3. Implement the **Smart Flip Algorithm**:
    * Define a `const SAFE_HEIGHT_PX = 260;` (An estimated buffer to clear the poster, title, and year comfortably).
    * Calculate `spaceAbove` using `rect.top`.
    * Calculate `spaceBelow` using `window.innerHeight - rect.bottom`.
    * **The Logic:** Add the `tooltip-down` class IF `spaceAbove < SAFE_HEIGHT_PX` AND `spaceBelow > spaceAbove`. Otherwise, remove it (default to popping up).

**Phase 2: Refactor `composer_profile.html` (Global Portal Collision)**
1. Locate the `recalcPortal()` function inside the `drawMarkers()` loop.
2. The current logic determines direction purely by `const goUp = (effectiveAbove >= effectiveBelow);`. This causes unnecessary squashing.
3. Replace this with a refined **Smart Flip Algorithm**:
    * Define a `const PREFERRED_HEIGHT = 320;` (Estimated max height of the portal tooltip).
    * **The Logic:** * Start with the assumption `let goUp = true;`
      * If `effectiveAbove < PREFERRED_HEIGHT` AND `effectiveBelow > effectiveAbove`, set `goUp = false`.
4. Leave the dynamic `maxHeight` clamping logic intact as a secondary failsafe, but this new directional check will ensure the tooltip prioritizes the direction that doesn't require clamping.

**Validation Rules for Agent:**
* Ensure `window.innerHeight` is used strictly for calculating viewport space, not document height.
* Do not alter the CSS structural logic (the `.tooltip-down` class in `composers.html` and the `top/bottom` inline styles in `composer_profile.html` are correct, only the JS triggering them needs to change).
* Do not introduce any new global variables; scope the calculations inside the existing hover/recalc functions.

