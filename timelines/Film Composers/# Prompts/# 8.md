

# Agent Task: "Sky & Ground" Layout Restructure

**Context:** The downward-hanging highlight labels are colliding with the waveform stacks. We are pivoting to a "Sky and Ground" design layout. The `.year-axis` must be moved *below* the `.timeline-track`. All "Most Important Film" labels must point *upward* into the empty space above the timeline. 

**File to Modify:** `composer_profile.html`

**Instructions:**

### 1. Reorder the DOM Elements
In the static HTML body, swap the order of the axis and the track so the axis sits below the waveform.
**Change this:**
```html
    <div id="timeline-container">
        <div class="year-axis" id="year-axis"></div>
        <div class="timeline-track" id="track"></div>
    </div>
```
**To this:**
```html
    <div id="timeline-container">
        <div class="timeline-track" id="track"></div>
        <div class="year-axis" id="year-axis"></div>
    </div>
```

### 2. Update CSS for the "Sky"
Locate the `<style>` block and make these updates:
* In `#timeline-container`, change `padding-bottom: 200px;` to **`padding-top: 250px; padding-bottom: 80px;`** (This creates the "sky" for the labels).
* In `.marker-imp-container`, change `top: 15px;` back to **`bottom: 15px;`** and ensure `top: auto;` is set.
* In `.year-axis`, add **`margin-top: 30px;`** to give it breathing room below the track.

### 3. Re-Invert HTML inside `drawMarkers`
Locate the block where `highlight.innerHTML` is defined. We need the label on top of the pole again.
**Set it to:**
```javascript
                    highlight.innerHTML = `
                        <div class="marker-label">${film.title}</div>
                        <div class="marker-pole"></div>
                    `;
```

### 4. Protect the Axis (Bottom Margin Fix)
Locate the dynamic margin calculation (Step 1.5 in `drawMarkers`) that we added previously. We need it to protect the *bottom* axis now.
**Update it to this:**
```javascript
            // 1.5 Calculate dynamic bottom margin to prevent axis collision
            if (columns.length > 0) {
                const maxStack = Math.max(...columns.map(col => col.length));
                const spacing = 16; 
                const maxDownwardReach = (maxStack / 2) * spacing;
                const safeMargin = Math.max(30, maxDownwardReach + 15);
                
                // Push the axis down by adding margin-bottom to the track
                track.style.marginBottom = `${safeMargin}px`;
            }
```

### 5. Update the Collision Logic (Build Upwards)
Locate `resolveSmartCollisions`. Replace the vertical stacking and edge-bounding logic inside the `containers.forEach` loop with this updated version that builds upwards and forces a tall enough base pole to clear the waveform:

```javascript
                // 1. Vertical Stacking (Building Upwards)
                box.style.bottom = '15px'; // Anchor to top of dot
                box.style.top = 'auto';

                // Force a tall base pole so labels clear the top of the waveform
                const basePole = 70; 
                const step = 40; // Lane height
                const newPoleHeight = basePole + (laneIndex * step);
                pole.style.height = `${newPoleHeight}px`;

                box.style.zIndex = 100 + laneIndex;

                // 2. Horizontal Edge Bounding
                const viewportWidth = window.innerWidth;
                const safetyPadding = 15; 
                let currentTransform = -50; 
                let pixelOffset = 0; 

                if (left < safetyPadding) {
                    pixelOffset = safetyPadding - left;
                } else if (right > (viewportWidth - safetyPadding)) {
                    pixelOffset = (viewportWidth - safetyPadding) - right;
                }

                if (pixelOffset !== 0) {
                    box.style.transform = `translateX(calc(${currentTransform}% + ${pixelOffset}px))`;
                } else {
                    box.style.transform = `translateX(${currentTransform}%)`;
                }
```

