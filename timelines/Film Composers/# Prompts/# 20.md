
### Agent Task Prompt

**Context:**
Our `scripts/audit_youtube_links.py` script is throwing the following error when attempting to fetch metadata for certain videos: `Requested format is not available. Use --list-formats for a list of available formats`. This is happening because `yt-dlp` is strictly evaluating video formats even though we only need text metadata.

Please fix the `yt-dlp` configuration in `scripts/audit_youtube_links.py`:

#### Task 1: Update `ydl_opts`
Locate the `ydl_opts` dictionary definition at the top of the `audit_links()` function.
1. **Remove** the line `'format': 'best',` entirely. We do not want to enforce any format selection.
2. **Add** the following key-value pairs to the dictionary to ensure it strictly extracts metadata without failing on format or playlist errors:
   ```python
   'extract_flat': True, # Forces metadata-only extraction
   'ignoreerrors': True, # Prevents crashing on minor video-specific errors
   ```

#### Task 2: Update the Error Handler
Locate the `try/except` block inside the main loop:
```python
# Handle the specific "format unavailable" error which can happen with cookies
if "format is not available" in error_msg.lower():
```
You can **delete** this specific `if` block. Now that we are ignoring formats, we won't get this specific error, and if the video is truly broken, it should fall through to the standard "Video unavailable or private" block and mark the link as `null`.

Please apply these updates and save the file.